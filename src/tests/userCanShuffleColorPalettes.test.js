describe(`
  User can shuffle color palettes by
  clicking any boxes on the screen.
`, () => {
  it('should make sure onclick event is fired', async () => {
    expect(true).toBe(true);
  });

  it('should shuffle CSS variables at :root', async () => {
    expect(true).toBe(true);
  });
});